{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Docker",
            "type": "shell",
            "command": "docker compose up --build",
            "problemMatcher": []
        }
    ]
}